----------------------------------------------------------------------------
SyharikFW by Syharik3316 V2.0
----------------------------------------------------------------------------

## Описание

Данный проект - firewall (Брандмауэр) для систем Linux поддерживающих библиотеку eBPF.

### Функционал:
1. Блокировка http/https трафика на всех портах кроме разрешенных
2. Проверка http трафика на стандартные протоколы (GET DELETE POST...)
3. Блокировка всех пакетов кроме L7
4. Блокировка DNS
5. Блокировка ICMP
6. Полное управление программой с помощью удобного веб интерфейса
7. Редактирование конфигурации и проверки логов через командную строку
8. Редактирование конфигурационного файла через команды в терминале
9. Логирование блокировок в отдельный файл и вывод их на окно терминала и веб интерфейса

----------------------------------------------------------------------------

## Требования

### Системные требования:
- Linux с поддержкой eBPF/XDP (ядро 4.18+)
- Компилятор C (gcc или clang)
- libbpf библиотека
- linux-tools-common
- Node.js (версия 14+)

### Системные зависимости:

#### Ubuntu/Debian:
```bash
sudo apt update
sudo apt install -y clang llvm linux-tools-common libbpf-dev libelf-dev zlib1g-dev \
    build-essential linux-headers-$(uname -r) nodejs
```

#### CentOS/RHEL/Fedora:
```bash
sudo dnf install -y clang llvm linux-tools-common libbpf-devel elfutils-libelf-devel \
    zlib-devel kernel-devel-$(uname -r) gcc make nodejs
```

### Node.js зависимости:
```bash
npm install
```

----------------------------------------------------------------------------

## Установка и компиляция

### 1. Клонирование/распаковка проекта

```bash
cd SyharikFWLocalBuild
```

### 2. Компиляция firewall

```bash
make
```

Если компиляция успешна, будет создан исполняемый файл `firewall`.

### 3. Установка firewall (опционально)

```bash
sudo make install
```

Это установит `firewall` в `/usr/local/bin/firewall`.

### 4. Установка Node.js зависимостей

```bash
npm install
```

----------------------------------------------------------------------------

## Настройка переменных окружения

Перед запуском веб-сервера вы можете настроить следующие переменные окружения:

### Переменные авторизации:

```bash
# Логин администратора (по умолчанию: admin)
export ADMIN_USERNAME=admin

# Пароль администратора (по умолчанию: admin)
export ADMIN_PASSWORD=admin

# Секретный ключ для сессий (изменить!)
export SESSION_SECRET=your-secret-key-here
```

### Переменные для автоматического ввода sudo пароля:

```bash
# Пароль sudo для автоматического ввода (опционально)
# Если не установлен, система будет запрашивать пароль вручную при первом запуске
export SUDO_PASSWORD=your_sudo_password
```

### Переменные сервера:

```bash
# Порт веб-сервера (по умолчанию: 3000)
export PORT=3000

# Имя systemd unit (по умолчанию: syharikfw@)
export SYFW_UNIT=syharikfw@
```

### Пример полной настройки:

Создайте файл `.env` в корне проекта:

```bash
# .env файл
ADMIN_USERNAME=admin
ADMIN_PASSWORD=my_secure_password
SUDO_PASSWORD=my_sudo_password
PORT=3000
SESSION_SECRET=change-this
```

Затем загрузите переменные перед запуском:

```bash
source .env
npm start
```

Или используйте `export` для каждой переменной:

```bash
export ADMIN_USERNAME=admin
export ADMIN_PASSWORD=my_secure_password
export SUDO_PASSWORD=my_sudo_password
export PORT=3000
npm start
```

----------------------------------------------------------------------------

## Запуск проекта

### Запуск веб-сервера:
```bash
npm start
```

Веб-интерфейс будет доступен по адресу: **http://localhost:3000**

При первом открытии вы увидите страницу авторизации:
- **Логин по умолчанию**: `admin`
- **Пароль по умолчанию**: `admin`

### Запуск firewall:

#### Через веб-интерфейс:
1. Откройте http://localhost:3000
2. Войдите в систему
3. Перейдите в "Управление firewall'ом"
4. Укажите интерфейс (например, `eth0`, `lo`)
5. Нажмите "Запустить"

#### Через командную строку:
```bash
sudo ./firewall --run [interface]
```

Например:
```bash
sudo ./firewall --run eth0
```

#### Через systemd (если настроен):
```bash
sudo systemctl start syharikfw@eth0
```

----------------------------------------------------------------------------

## Настройка systemd

### 1. Скомпилируйте и установите firewall:

```bash
make
sudo make install
```

### 2. Создайте unit-файл:

```bash
sudo cp syharikfw@.service /etc/systemd/system/
```

Или используйте скрипт настройки:
```bash
sudo ./setup-systemd.sh
```

### 3. Отредактируйте unit-файл:

```bash
sudo nano /etc/systemd/system/syharikfw@.service
```

**ВАЖНО**: Обязательно измените:

- **ExecStart**: Если вы НЕ устанавливали через `sudo make install`, замените `/usr/local/bin/firewall` на полный путь к скомпилированному файлу.
  Например: `ExecStart=/home/user/SyharikFWLocalBuild/firewall --run %i`

- **WorkingDirectory**: Замените `/path/to/firewall` на фактический путь к директории проекта, где находятся `firewall.conf`, `firewall.log` и другие файлы.
  Например: `WorkingDirectory=/home/user/SyharikFWLocalBuild`

### 4. Перезагрузите systemd:

```bash
sudo systemctl daemon-reload
```

### 5. Запуск/остановка/статус:

```bash
# Запуск для интерфейса eth0
sudo systemctl start syharikfw@eth0

# Остановка
sudo systemctl stop syharikfw@eth0

# Статус
sudo systemctl status syharikfw@eth0

# Автозапуск при загрузке системы
sudo systemctl enable syharikfw@eth0
```

Веб-интерфейс автоматически попытается использовать systemd (если доступен) через `systemctl start/stop`. При отсутствии systemd используется прямой запуск firewall.

----------------------------------------------------------------------------

## Использование веб-интерфейса

### Авторизация:
1. Откройте http://localhost:3000
2. Введите логин и пароль (по умолчанию: `admin` / `admin`)
3. После успешного входа вы попадете на главную страницу

### Управление портами:

#### Стандартные порты:
По умолчанию в системе настроены следующие порты:
- **22** - SSH
- **53** - DNS
- **80** - HTTP
- **443** - HTTPS
- **3000** - SyharikFW Web Server

#### Добавление порта:
1. Нажмите кнопку "Добавить порт"
2. Введите номер порта (1-65535)
3. Нажмите "Добавить"

#### Удаление порта:
1. Нажмите кнопку "Удалить порт"
2. Введите номер порта для удаления
3. Нажмите "Удалить"

#### Просмотр списка портов:
- Список портов отображается автоматически
- Если портов больше 10, появится кнопка "Развернуть список" для просмотра всех портов

**⚠️ ВАЖНО**: Нельзя изменять порты во время работы firewall! Сначала остановите firewall, затем изменяйте порты.

### Управление firewall:

1. Нажмите кнопку "Управление firewall'ом"
2. Введите имя интерфейса (например: `eth0`, `lo`, `ens33`)
3. Нажмите "Запустить" для запуска или "Остановить" для остановки

### Просмотр логов:

1. Нажмите кнопку "Просмотр логов" или перейдите на http://localhost:3000/logs.html
2. Логи обновляются автоматически каждые 5 секунд
3. Отображаются последние 200 строк логов
4. Если логов больше 50, появится кнопка для разворачивания полного списка

### Выход из системы:

Нажмите кнопку выхода (🚪) в правом верхнем углу для выхода из системы.

----------------------------------------------------------------------------

## Использование командной строки

### Запуск firewall:
```bash
sudo ./firewall --run [interface]
```

### Управление портами:
```bash
# Добавить порты
sudo ./firewall --add 8080,9090

# Удалить порт
sudo ./firewall --del 8080

# Показать список портов
./firewall --list
```

### Проверка статуса:
```bash
./firewall --status
```

### Остановка:
```bash
sudo ./firewall --detach [interface]
```

Или через systemd:
```bash
sudo systemctl stop syharikfw@eth0
```

### Справка:
```bash
sudo ./firewall --help
```

----------------------------------------------------------------------------

## Структура проекта

```
SyharikFWLocalBuild/
├── firewall.c              # Основной код firewall на C
├── firewall.bpf.c          # BPF программа
├── server.js               # Node.js веб-сервер
├── fd.html                 # Главная страница веб-интерфейса
├── logs.html               # Страница просмотра логов
├── login.html              # Страница авторизации
├── style.css               # Стили интерфейса
├── package.json            # Node.js зависимости
├── data.json               # Сохраненное состояние портов
├── firewall.conf           # Конфигурационный файл (создается автоматически)
├── firewall.log            # Файл логов (создается автоматически)
├── Makefile                # Файл сборки
├── syharikfw@.service      # Systemd unit файл
├── setup-systemd.sh        # Скрипт настройки systemd
├── README.md               # Этот файл
```

----------------------------------------------------------------------------

## Устранение проблем

### Ошибка компиляции BPF программы
Убедитесь, что установлены заголовки ядра:
```bash
sudo apt install linux-headers-$(uname -r)
```

### Ошибка "Permission denied" при запуске
Firewall требует root-прав для работы с XDP. Используйте `sudo`.

### Ошибка "Interface not found"
Проверьте имя интерфейса:
```bash
ip link show
```

### BPF программа не загружается
Убедитесь, что:
- Ядро поддерживает eBPF (проверьте `/sys/fs/bpf`)
- Установлены все зависимости libbpf
- Используется правильная версия bpftool

### Ошибка "Cannot modify ports while firewall is running"
Остановите firewall перед изменением портов:
1. Перейдите в "Управление firewall'ом"
2. Нажмите "Остановить"
3. Дождитесь изменения статуса на OFFLINE
4. Теперь можно изменять порты

### Веб-сервер не запускается
Проверьте:
- Установлен ли Node.js: `node --version`
- Установлены ли зависимости: `npm install`
- Не занят ли порт 3000: `lsof -i :3000`

### Проблемы с авторизацией
- Убедитесь, что переменные окружения установлены правильно
- Проверьте, что используется правильный логин/пароль
- Очистите cookies браузера и попробуйте снова

### Sudo пароль не вводится автоматически
- Убедитесь, что переменная `SUDO_PASSWORD` установлена
- Проверьте правильность пароля

### Логи не отображаются
- Убедитесь, что firewall запущен и работает
- Проверьте наличие файла `firewall.log` в директории проекта
- Проверьте права доступа к файлу логов

----------------------------------------------------------------------------

## Тестирование

Проект тестировался на:
- **ОС**: Ubuntu 22.04.3
- **Ядро**: Linux 5.15+
- **Node.js**: 14+

----------------------------------------------------------------------------

## Оригинальный проект

[Оригинальный проект V1.0 Alpha](https://github.com/Syharik3316/Firewall-V1.0-Alpha) - был создан по мотивам кейса компании ООО "МАДРИГАЛ"

Их сайт: [мадригал.рус](http://мадригал.рус)

----------------------------------------------------------------------------

## Лицензия

Проект разработан командой Syharik3316.

----------------------------------------------------------------------------

## Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение проблем"
2. Убедитесь, что все зависимости установлены
3. Проверьте логи firewall: `cat firewall.log`
4. Проверьте логи веб-сервера в консоли

----------------------------------------------------------------------------
